services:
  db:
    image: postgis/postgis:16-3.4-alpine
    container_name: brainberg-db
    restart: always
    environment:
      POSTGRES_DB: brainberg_dev
      POSTGRES_USER: brainberg
      POSTGRES_PASSWORD: devpassword
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U brainberg -d brainberg_dev"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
    driver: local
